from ultralytics import YOLO

# Load pretrained YOLOv8 pose model (nano = fastest)
model = YOLO("yolov8n-pose.pt")

# Train on your dataset
model.train(
    data="data.yaml",
    epochs=50,
    imgsz=640
)
